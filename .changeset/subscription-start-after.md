---
'@iota/iota-sdk': minor
'@iota/graphql-transport': minor
---

Add `startAfter` to `subscribeEvent` and `subscribeTransaction` to resume a subscription after a given transaction digest over GraphQL, and fill `id.txDigest` on subscribed events
